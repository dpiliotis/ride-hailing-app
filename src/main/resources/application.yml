spring:
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: create-drop

  datasource:
    url: jdbc:mysql://localhost:3306/ride_db?charSet=utf8&serverTimezone=UTC
    username: root
    password: password
    initialization-mode: always